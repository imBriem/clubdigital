/* import React from 'react';


const { Provider } = AppContext;

export const AppProvider = (props) => {

  const [message, setMessage] = React.useState("My name is context");

return(

   <Provider value={[message, setMessage]}>

      {props.children}

   </Provider>

 );

} */
